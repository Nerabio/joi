version: '3.8'

services:
  app:
    build: .
    ports:
      - '3000:3000'
    volumes:
      - ./config.json:/app/config.json
    environment:
      - NODE_ENV=production
    restart: unless-stopped
